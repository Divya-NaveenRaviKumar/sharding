CREATE TABLE employee
(
    id                 int                     PRIMARY KEY,
    name               TEXT                     NOT NULL,
    description        TEXT                     NOT NULL
);
